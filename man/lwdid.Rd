% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lwdid.R
\name{lwdid}
\alias{lwdid}
\title{Lee-Wooldridge DiD via unit-specific pre-treatment transformations}
\usage{
lwdid(
  data,
  y,
  ivar,
  tvar,
  gvar = NULL,
  post = NULL,
  dvar = NULL,
  rolling = "demean",
  method = NULL,
  control_group = "never_treated",
  aggregate = "overall",
  vce = NULL,
  cluster_var = NULL,
  controls = NULL,
  season_var = NULL,
  pre = -1L,
  never = FALSE,
  attgt = FALSE,
  ydot = FALSE,
  reps = 999L,
  seed = NULL,
  level = 95,
  nose = FALSE,
  nboot = 999,
  nperm = 999,
  vce_inner = "hc3"
)
}
\arguments{
\item{data}{A long-format panel data frame (one row per unit-period).}

\item{y}{Character. Name of the outcome column.}

\item{ivar}{Character. Name of the unit identifier column.}

\item{tvar}{Character. Name of the calendar time column (numeric or integer).}

\item{gvar}{Character or NULL. Name of the first-treatment-year column for
staggered designs. Units with value \code{0} or \code{NA} are treated as never-treated.
Set to \code{NULL} (default) for common-timing designs (supply \code{post} instead).}

\item{post}{Character or NULL. Name of a binary post-treatment indicator column
(0 = pre, 1 = post). Required when \code{gvar = NULL}; ignored otherwise.}

\item{dvar}{Character or NULL. Name of a unit-level treatment-group indicator
for common-timing designs. If omitted, \code{lwdid()} keeps backwards-compatible
behavior when \code{post} is the treatment-on indicator; if \code{post} is a calendar
post indicator, it looks for an unambiguous unit-invariant treatment column
named \code{treat}, \code{treated}, \code{D}, or \code{d}.}

\item{rolling}{Character. Transformation method applied to each unit's
pre-treatment observations:
\describe{
\item{\code{"demean"}}{Subtract the unit's pre-period mean (default).}
\item{\code{"detrend"}}{Remove a linear trend fitted on pre-periods.}
\item{\code{"demeanq"}}{Seasonal demeaning; requires \code{season_var}.}
\item{\code{"detrendq"}}{Seasonal detrending; requires \code{season_var}.}
}}

\item{method}{Character or NULL. If one of \code{"ra"}, \code{"ipw"}, \code{"ipwra"}, the
large-N path (Lee & Wooldridge 2026a) is used: per-cohort/period ATT(g,t)
via regression adjustment, inverse-probability weighting, or doubly-robust
IPWRA, aggregated to the event-study WATT(r) path with wild-cluster-bootstrap
inference. \code{ipw}/\code{ipwra} require \code{controls}. \code{rolling} must be \code{"demean"} or
\code{"detrend"}. If \code{NULL} (default), the small-N path is used.}

\item{control_group}{Character. Control group for staggered designs:
\code{"never_treated"} (default) or \code{"not_yet_treated"}.}

\item{aggregate}{Character. Aggregation level for staggered designs:
\code{"overall"} (default), \code{"cohort"}, or \code{"none"} (returns all (g,r) pairs).}

\item{vce}{Character or NULL. Variance-covariance estimator:
\code{NULL} (homoskedastic OLS), \code{"hc1"}, \code{"hc3"}, \code{"cluster"},
\code{"wildboot"} (wild cluster bootstrap), or \code{"permutation"} (randomisation
inference). The last two are distribution-free and recommended at small N.}

\item{cluster_var}{Character or NULL. Column name for clustering; required
when \code{vce = "cluster"}.}

\item{controls}{Character vector or NULL. Names of time-invariant control
variables to include in the cross-sectional regression.}

\item{season_var}{Character or NULL. Column name of the seasonal indicator
(required for \code{rolling = "demeanq"} or \code{"detrendq"}).}

\item{pre}{Integer. Large-N only. Number of most-recent pre-periods used in the
transformation; \code{-1} (default) uses all pre-periods.}

\item{never}{Logical. Large-N only. If \code{TRUE}, the comparison group is
never-treated units only (default \code{FALSE} uses not-yet-treated units too).}

\item{attgt}{Logical. Large-N only. If \code{TRUE}, the ATT(g,t) cell estimates are
returned (and printed).}

\item{ydot}{Logical. Large-N only. If \code{TRUE}, the per-cohort transformed
outcomes are returned.}

\item{reps}{Integer. Large-N only. Wild cluster bootstrap replications (default 999).}

\item{seed}{Integer or NULL. Large-N only. Seed for the wild bootstrap.}

\item{level}{Numeric. Confidence level for the large-N path (default 95).}

\item{nose}{Logical. Large-N only. If \code{TRUE}, skip standard errors (faster).}

\item{nboot}{Integer. Number of bootstrap replications for \code{vce = "wildboot"}
(default 999).}

\item{nperm}{Integer. Number of permutations for \code{vce = "permutation"}
(default 999).}

\item{vce_inner}{Character. Inner variance estimator used when computing the
observed t-statistic inside the wild bootstrap (default \code{"hc3"}).}
}
\value{
An object of class \code{"lwdid"}, a list containing:
\describe{
\item{\code{design}}{\code{"staggered"} or \code{"common_timing"}.}
\item{\code{att_overall}}{Estimated overall ATT.}
\item{\code{se_overall}}{Standard error of overall ATT.}
\item{\code{tstat}}{t-statistic.}
\item{\code{pvalue}}{Two-sided p-value.}
\item{\code{att_by_cohort}}{Data frame of cohort-specific ATTs (staggered only).}
\item{\code{att_by_cohort_time}}{Data frame of (g,r)-specific ATTs (staggered only).}
\item{\code{att_by_period}}{Data frame of period-specific ATTs (common timing only).}
\item{\code{ci_lower}, \code{ci_upper}}{95\% confidence interval bounds (common timing only).}
\item{\code{N}}{Sample size at first post-treatment period (common timing only).}
}
}
\description{
Main entry point implementing the Lee & Wooldridge (2026) panel
difference-in-differences estimator. Supports both common-timing and
staggered adoption designs. Each unit's outcome is residualised using
only its own pre-treatment observations before running a pooled
cross-sectional OLS.
}
\examples{
# Load bundled Castle Doctrine dataset
castle <- read.csv(system.file("extdata", "castle.csv", package = "lwdidR"))
castle$gvar <- castle$effyear
castle$gvar[is.na(castle$gvar) | castle$gvar == 0] <- NA

# Staggered design with demeaning and HC3 standard errors
res <- lwdid(castle, "lhomicide", "sid", "year",
             gvar = "gvar", rolling = "demean", vce = "hc3")
print(res)

}
\references{
Lee, S. J., & Wooldridge, J. M. (2026a). Simple Transformation Approach to
Difference-in-Differences Estimation for Panel Data. Journal of Business &
Economic Statistics, 1-27. \doi{10.1080/07350015.2026.2683047}

Lee, S. J., & Wooldridge, J. M. (2026b). Simple Approaches to Inference with
Difference-in-Differences Estimators with Small Cross-Sectional Sample Sizes.
Working paper, SSRN 5325686.
\url{https://papers.ssrn.com/sol3/papers.cfm?abstract_id=5325686}

\code{lwdidR} is an R port of the original Stata \code{lwdid} command by
Soo Jeong Lee and Jeffrey M. Wooldridge:
\url{https://github.com/Soo-econ/lwdid}. The Castle Doctrine example (Section
7.2) and the small-sample inference options are from (2026b).
}
